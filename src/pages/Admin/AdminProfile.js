import React from 'react';
import UserProfile from '../User/UserProfile';

// Reuse the same profile UI for admin for now
const AdminProfile = () => {
  return <UserProfile />;
};

export default AdminProfile;
